{
  "name": "program-language-detector",
  "version": "0.0.1",
  "description": "A program language detector",
  "repository": {
    "url": "ULIVZ/program-language-detector",
    "type": "git"
  },
  "main": "dist/htmlCssTransformer.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test:cov": "jest --coverage && npm run lint",
    "test": "npm run lint && jest",
    "lint": "xo",
    "prepublish": "npm run build",
    "build": "bili --format cjs,es,umd,umd-min --exports default --name programLanguageDetector"
  },
  "author": "ULIVZ <chl814@foxmail.com>",
  "license": "MIT",
  "jest": {
    "testEnvironment": "node"
  },
  "babel": {
    "env": {
      "test": {
        "presets": [
          [
            "env",
            {
              "targets": {
                "node": "current"
              }
            }
          ]
        ]
      }
    }
  },
  "dependencies": {
    "lang-detector": "^1.0.6"
  },
  "devDependencies": {
    "jest-cli": "^19.0.0",
    "eslint-config-rem": "^3.0.0",
    "xo": "^0.18.0",
    "babel-preset-env": "^1.4.0",
    "bili": "^2.2.3"
  },
  "xo": {
    "extends": "rem",
    "envs": [
      "jest"
    ]
  }
}
